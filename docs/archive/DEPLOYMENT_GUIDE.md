# VIDIST - คู่มือนำขึ้นโดเมน (สำหรับมือใหม่)

> วิธีการนำ VIDIST ขึ้นโดเมนเพื่อใช้งานบนมือถือได้ทุกที่

---

## 📋 สิ่งที่ต้องเตรียม

- [ ] โดเมน (ซื้อจาก Namecheap, GoDaddy หรือ Cloudflare) - ไม่มีก็ได้
- [ ] บัญชี GitHub (สมัครฟรีที่ github.com)
- [ ] ไฟล์โปรเจ็ค VIDIST ทั้งหมด
- [ ] API Keys ของคุณ:
  - Wavespeed API Key
  - Google Gemini API Key (สำหรับ Prompt Craft)

---

## 🚀 วิธีที่ 1: Cloudflare Pages (แนะนำ - ฟรี, เร็ว, ง่าย)

### ขั้นตอนทำ:

**1. อัปโหลดโค้ดไปยัง GitHub**
```bash
# เปิด Command Prompt หรือ Terminal
cd d:\for_back_home2026\Vidist
git init
git add .
git commit -m "นำ VIDIST ขึ้นโดเมน"
git remote add origin https://github.com/USERNAME/vidist.git
git push -u origin main
```

**2. เชื่อมต่อกับ Cloudflare Pages**
- เข้า https://pages.cloudflare.com/
- กด "Create a project" → "Connect to Git"
- เลือก repository ชื่อ `vidist`
- **ตั้งค่า Build:**
  - Framework: เลือก `None`
  - Build command: เว้นว่างไว้
  - Output directory: พิมพ์ `/`
- กด "Save and Deploy"

**3. รอให้ Deploy เสร็จ (1-2 นาที)**
- Cloudflare จะสร้าง URL ให้คุณ เช่น `vidist-abc123.pages.dev`
- เปิดได้ทั้งมือถือและคอมพิวเตอร์เลย

**4. เชื่อมโดเมนของคุณเอง (ถ้ามี)**
- ไปที่ "Custom domains" → "Set up a custom domain"
- พิมพ์โดเมน: `vidist.yourdomain.com`
- ทำตาม DNS settings ที่ Cloudflare แนะนำ
- รอประมาณ 5-30 นาที

### ✅ เสร็จแล้ว! เข้าใช้ได้ที่ `https://vidist.yourdomain.com`

---

## 🚀 วิธีที่ 2: Netlify (ฟรี, แค่ลาก-วาง)

### ขั้นตอนทำ:

**1. บีบอัดไฟล์โปรเจ็ค**
- เปิด folder `d:\for_back_home2026\Vidist`
- เลือกไฟล์ทั้งหมด → คลิกขวา → "ส่งไปยัง" → "โฟลเดอร์ที่บีบอัด (zip)"

**2. อัปโหลดด้วย Netlify Drop**
- เข้า https://app.netlify.com/drop
- ลากไฟล์ .zip วางในกล่อง
- รอให้อัปโหลด + deploy เสร็จ (1-2 นาที)

**3. รับ URL ฟรี**
- Netlify สร้าง URL ให้: `random-name-123.netlify.app`
- เปลี่ยนชื่อได้ที่ "Site settings" → "Change site name"

**4. เชื่อมโดเมนของคุณ (ถ้ามี)**
- ไปที่ "Domain settings" → "Add custom domain"
- พิมพ์โดเมน + ตั้งค่า DNS ตามที่บอก

### ✅ เสร็จแล้ว! เข้าใช้ได้ที่ `https://vidist.netlify.app`

---

## 🚀 วิธีที่ 3: Vercel (ฟรี, รวดเร็วมาก)

### ขั้นตอนทำ:

**1. Push ไปยัง GitHub** (เหมือนวิธีที่ 1)

**2. Import ใน Vercel**
- เข้า https://vercel.com/new
- กด "Import Git Repository"
- เลือก repository `vidist`
- **ตั้งค่า:**
  - Framework: เลือก `Other`
  - Build Command: เว้นว่าง
  - Output Directory: พิมพ์ `.`
- กด "Deploy"

**3. รอ Deploy (30-60 วินาที)**
- Vercel สร้าง URL: `vidist-xxx.vercel.app`

**4. เชื่อมโดเมนของคุณ**
- ไปที่ "Settings" → "Domains" → "Add"
- พิมพ์โดเมน + ตั้งค่า DNS

### ✅ เสร็จแล้ว! เข้าใช้ได้ที่ `https://vidist.vercel.app`

---

## 📱 ใช้งานบนมือถือ

### วิธีเข้าใช้:

**1. เปิดเบราว์เซอร์บนมือถือ**
- iOS: Safari
- Android: Chrome

**2. เข้า URL ของคุณ**
- พิมพ์ `https://vidist.yourdomain.com` (หรือ URL ที่ได้จาก Cloudflare/Netlify/Vercel)

**3. เพิ่มไปหน้าจอหลัก (ให้เหมือน App)**
- **iPhone/iPad:**
  1. กดปุ่ม "แชร์" (ไอคอนลูกศร)
  2. เลื่อนหา "เพิ่มไปยังหน้าจอหลัก"
  3. กด "เพิ่ม"

- **Android:**
  1. กดเมนู (จุด 3 จุดมุมขวาบน)
  2. เลือก "เพิ่มไปยังหน้าจอหลัก"
  3. กด "เพิ่ม"

### ข้อดีของการใช้บนมือถือ:

- ✅ เหมือนใช้ App จริงๆ ไม่ต้องติดตั้ง
- ✅ เปิดเร็ว ใช้งานลื่นไหล
- ✅ ฟีเจอร์ครบทุกอย่าง (เหมือนใช้บน PC)
- ✅ บันทึก API keys, history ไว้ในเครื่อง
- ✅ รองรับการสัมผัส (swipe Red Mode, drag & drop รูปภาพ)
- ✅ ใช้ได้ทุกที่ ทุกเวลา

---

## 🔒 วิธีรักษาความปลอดภัย API Keys

### ⚠️ สิ่งสำคัญที่ต้องรู้:

VIDIST เป็น **client-side app** (ทำงานผ่าน JavaScript บนเบราว์เซอร์)
- API keys จะถูกเก็บใน localStorage ของเบราว์เซอร์คุณ
- ไม่ได้เก็บบนเซิร์ฟเวอร์

### วิธีป้องกัน API keys:

**1. ใช้โดเมนส่วนตัว**
- อย่าแชร์ URL ให้คนอื่น
- ตั้งชื่อโดเมนที่ไม่เดาง่าย
- ตัวอย่าง: `my-private-vidist-2025.pages.dev`

**2. ตั้งค่า Cloudflare Access (วิธีที่ปลอดภัยที่สุด - แนะนำ)**
- เข้า Cloudflare Dashboard
- ไปที่ "Zero Trust" → "Access"
- สร้าง Application policy สำหรับโดเมนของคุณ
- เลือก "One-time PIN" หรือ "Email authentication"
- **ผลลัพธ์:** เฉพาะคุณเท่านั้นที่เข้าถึงได้

**3. จำกัดโดเมนใน Wavespeed API**
- เข้า Wavespeed Dashboard
- ไปที่ API Key settings
- ตั้งค่า "Allowed domains": `vidist.yourdomain.com`
- ตั้งค่า "Request rate limit" ตามที่เหมาะสม

**4. จำกัดโดเมนใน Gemini API**
- เข้า https://aistudio.google.com/apikey
- เลือก API key ของคุณ → "Edit restrictions"
- เลือก "Application restrictions" → "HTTP referrers"
- เพิ่ม: `https://vidist.yourdomain.com/*`

---

## 🔄 วิธีอัปเดตโปรเจ็ค

### สำหรับ Cloudflare Pages / Vercel (ใช้ Git):

```bash
cd d:\for_back_home2026\Vidist
git add .
git commit -m "อัปเดต: [บอกว่าเปลี่ยนอะไร]"
git push origin main
```
→ ระบบจะ deploy ให้อัตโนมัติภายใน 1-2 นาที

### สำหรับ Netlify (แบบลาก-วาง):

- ลาก-วาง folder ใหม่ทับของเดิมใน Netlify Dashboard
- หรือเชื่อม Git repository แล้วใช้ auto-deploy

---

## ⚡ เทคนิคเพิ่มความเร็ว

### 1. ตั้งค่า Cache (Cloudflare)
- ไปที่ "Caching" → "Configuration"
- ตั้ง Browser Cache TTL: `1 year`
- ไฟล์ CSS, JS จะโหลดเร็วขึ้นมาก

### 2. เปิด Compression
- Cloudflare/Netlify/Vercel เปิดให้อัตโนมัติ
- ลดขนาดไฟล์ประมาณ 70%

### 3. CDN ทั่วโลก
- ทุกเว็บมี CDN ให้ฟรี
- ไฟล์จะถูกส่งจาก server ที่ใกล้คุณที่สุด

---

## 🐛 แก้ปัญหาที่พบบ่อย

### ปัญหา: API ไม่ทำงานบนมือถือ

**วิธีแก้:**
- ตรวจสอบว่าใช้ HTTPS (ต้องมี 🔒 หน้า URL)
- ลองล้าง cache เบราว์เซอร์
- ตรวจสอบ CORS settings ของ Wavespeed API

### ปัญหา: localStorage หายหลัง refresh

**วิธีแก้:**
- ห้ามใช้โหมดไม้กางเขน (Private/Incognito mode)
- Safari iOS: ไปที่ตั้งค่า → Safari → ปิด "Prevent Cross-Site Tracking"

### ปัญหา: วิดีโอไม่เล่นบนมือถือ

**วิธีแก้:**
- Wavespeed สร้างเป็น MP4 อยู่แล้ว รองรับทุกอุปกรณ์
- ตรวจสอบสัญญาณ internet
- ลองดาวน์โหลดวิดีโอแล้วเล่นในแกลเลอรี่

### ปัญหา: Prompt Craft ช้า

**วิธีแก้:**
- Gemini 2.0 Flash เร็วอยู่แล้ว
- ตรวจสอบความเร็ว internet
- ลดความยาว craft input (ไม่เกิน 500 ตัวอักษร)

---

## 📊 เปรียบเทียบเว็บโฮสติ้ง

| ฟีเจอร์ | Cloudflare Pages | Netlify | Vercel |
|---------|------------------|---------|--------|
| **ราคา** | ฟรี | ฟรี | ฟรี |
| **Bandwidth** | ไม่จำกัด | 100 GB/เดือน | 100 GB/เดือน |
| **โดเมนฟรี** | ✅ | ✅ | ✅ |
| **HTTPS อัตโนมัติ** | ✅ | ✅ | ✅ |
| **CDN ทั่วโลก** | 300+ เมือง | 100+ เมือง | 100+ เมือง |
| **ความเร็ว Deploy** | ⚡⚡⚡ (30 วินาที) | ⚡⚡ (60 วินาที) | ⚡⚡⚡ (30 วินาที) |
| **ป้องกัน DDoS** | ✅ | ✅ | ✅ |
| **วิเคราะห์สถิติ** | ✅ | ✅ | ✅ |
| **ความง่ายในการใช้** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |

**คำแนะนำ:** ใช้ **Cloudflare Pages** - ฟรี, เร็ว, bandwidth ไม่จำกัด

---

## ✅ Checklist ก่อน Deploy

ตรวจสอบให้ครบก่อนนำขึ้นโดเมน:

- [ ] ทดสอบทุกฟีเจอร์บนเครื่องให้ทำงานปกติ
- [ ] ตรวจสอบว่าไม่มี API keys ในโค้ด (ต้องใส่ผ่าน UI เท่านั้น)
- [ ] เปิด Console (กด F12) ดูว่าไม่มี error สีแดง
- [ ] ทดสอบบนมือถือ (Chrome DevTools → โหมดมือถือ)
- [ ] เตรียม Wavespeed API key
- [ ] เตรียม Gemini API key
- [ ] เลือกเว็บโฮสติ้ง (แนะนำ Cloudflare Pages)
- [ ] เตรียมโดเมน (ถ้ามี) หรือใช้ subdomain ฟรีที่เว็บให้มา
- [ ] อ่านวิธีรักษาความปลอดภัยข้างบน

---

## 🎯 สรุปแบบเร็ว (Quickstart)

### ขั้นตอนย่อสำหรับคนรีบ:

**1. อัปโหลดโค้ดไป GitHub:**
```bash
git init
git add .
git commit -m "Deploy VIDIST"
git remote add origin https://github.com/YOUR_USERNAME/vidist.git
git push -u origin main
```

**2. Deploy บน Cloudflare Pages:**
- เข้า https://pages.cloudflare.com/
- เชื่อม repository → กด Deploy
- รอ 1-2 นาที

**3. เริ่มใช้งาน:**
- เปิด URL: `https://vidist-xxx.pages.dev`
- ใส่ API keys (Wavespeed + Gemini)
- เพิ่มไปหน้าจอหลักมือถือ
- เริ่มสร้างวิดีโอได้เลย!

---

## 💡 เคล็ดลับเพิ่มเติม

### ทำให้เหมือน App มากขึ้น:

**1. ตั้งชื่อเว็บสั้นๆ จดจำง่าย**
- แทนที่จะเป็น `vidist-random-123.pages.dev`
- เปลี่ยนเป็น `vidist.pages.dev` หรือ `my-vidist.pages.dev`

**2. ใช้ไอคอนสวยๆ**
- ไฟล์ `favicon.ico` มีอยู่แล้วในโปรเจ็ค
- จะแสดงเป็นไอคอนเมื่อเพิ่มไปหน้าจอหลัก

**3. Bookmark ไว้ในเบราว์เซอร์**
- เพิ่มไปหน้าจอหลัก iOS/Android
- หรือเพิ่มเป็น bookmark ในเบราว์เซอร์
- เปิดได้รวดเร็วทันใจ

### ใช้งานหลายอุปกรณ์:

- ✅ **PC/Mac:** เปิดผ่านเบราว์เซอร์ตามปกติ
- ✅ **มือถือ:** เพิ่มไปหน้าจอหลัก ใช้เหมือน App
- ✅ **แท็บเล็ต:** รองรับทุกขนาดหน้าจอ
- ✅ **Sync:** API keys + History บันทึกอิสระในแต่ละเครื่อง

---

## 📞 แหล่งข้อมูลเพิ่มเติม

### เอกสารประกอบ:

- **Cloudflare Pages:** https://developers.cloudflare.com/pages/
- **Netlify Docs:** https://docs.netlify.com/
- **Vercel Docs:** https://vercel.com/docs
- **Wavespeed API:** https://wavespeed.ai/docs
- **Google Gemini API:** https://ai.google.dev/

### การช่วยเหลือ:

- **ปัญหาเกี่ยวกับ VIDIST:** สร้าง Issue ใน GitHub repository
- **ปัญหา Hosting:** ดู documentation ของแต่ละเว็บ
- **ปัญหา API:** ติดต่อ support ของ Wavespeed/Google

---

## 🎉 เริ่มต้นใช้งานได้เลย!

คุณพร้อมแล้วที่จะนำ VIDIST ขึ้นโดเมนและใช้งานบนมือถือทุกที่ทุกเวลา!

### ขั้นตอนสั้นๆ:
1. เลือกเว็บโฮสติ้ง (แนะนำ Cloudflare Pages)
2. Deploy โปรเจ็ค (1-2 นาที)
3. เปิดบนมือถือ
4. เพิ่มไปหน้าจอหลัก
5. เริ่มสร้างวิดีโอ AI ได้เลย!

**ใช้เวลาทั้งหมดไม่เกิน 5 นาที** จากไม่มีอะไรเลย ถึงมี App สร้างวิดีโอ AI บนมือถือของคุณเอง! 🚀

---

*สร้างโดย VIDIST v1.12.0 | อัปเดตล่าสุด: 29 ธันวาคม 2025*
